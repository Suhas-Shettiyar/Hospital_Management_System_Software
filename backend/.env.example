APP_NAME=MedCore HMS
APP_VERSION=0.1.0
# Frontend origin allowed to call this API (Vite dev server)
CORS_ORIGINS=http://localhost:5173
# PostgreSQL connection (optional for first run). Change CHANGE_ME to your postgres password.
DATABASE_URL=postgresql+psycopg://postgres:CHANGE_ME@localhost:5432/hms
